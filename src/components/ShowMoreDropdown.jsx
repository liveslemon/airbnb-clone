// This component is currently unused, but if you want to use it for a dropdown, you can implement it like this:
import React from 'react';

const ShowMoreDropdown = ({ children }) => {
  return (
    <div className="show-more-dropdown">
      {children}
    </div>
  );
};

export default ShowMoreDropdown;
